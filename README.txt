# Canny Edge Detection using Webcam

This Python project demonstrates the use of Canny Edge Detection on a real-time video feed captured from a webcam. The project uses the OpenCV library to process the video stream and display the results.

## Requirements

- Python 3.x
- OpenCV library (Install using `pip install opencv-python`)